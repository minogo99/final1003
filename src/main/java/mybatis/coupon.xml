<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="voucher.model.couponBean">
<select id="useCheck" resultType="int">
select count(*) from coupon where cp_number = #{cp_number} and cp_duedate > #{nowdate} 
</select>

<update id="couponRegister">
update coupon set cp_id = #{cp_id},cp_usedate=sysdate,cp_check='true' where cp_number = #{cp_number}
</update>

</mapper>