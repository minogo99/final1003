<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="member.model.MemberJjimBean">

<insert id="insertJjim">
	insert into jjim values(jjim_seq.nextval,#{movie_num},#{user_id})
</insert>

<delete id="deleteJjim">
	delete from jjim where movie_num = #{movie_num} and user_id = #{user_id}
</delete>

<select id="getByData" resultType="myJjim">
	select * from jjim where user_id=#{user_id}
</select>
</mapper>