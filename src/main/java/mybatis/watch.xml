<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="member.model.MemberWatchBean">
	
	<insert id="insertWatch">
	insert into watch values(watch_seq.nextval,#{movie_num},#{user_id})
	</insert>
	
	<select id="getByMemdata" resultType="MyCategory">
	select v.image from watch w inner join video v on w.movie_num = v.num where w.user_id = #{user_id}
	</select>
	
	<delete id="deleteWatch">
		delete from watch
	</delete>
	
	</mapper>