<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board.model.ReplyBean">
<select id="list" parameterType="int" resultType="board.model.ReplyBean">
	select * from (select rownum as rank,rno,bno,writer,content,reg_date from (select * from reply where bno = #{bno} order by reg_date desc)) where rank between #{startRow} and #{endRow}		
</select>
<insert id="replyWrite">
insert into reply values(reply_seq.nextval,#{bno},#{writer},#{content},sysdate)
</insert>
<select id="listCount" resultType="int">
select count(*) from reply where bno=#{bno}
</select>
</mapper>